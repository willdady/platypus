// Extending section order. Audience: plugin authors and contributors. The
// quickstart comes second so a first-time author reaches runnable code before
// any policy; compatibility policy lands last because it is the page you read
// once and then stop thinking about.
const meta = {
  index: "Overview",
  "your-first-plugin": "Your first plugin",
  "tool-sets": "Tool sets",
  "sandbox-backends": "Sandbox backends",
  "plugin-api-and-config": "Plugin API & config",
};

export default meta;
