---
title: Extending
description: What you can add to Platypus without forking it, and how to pick the right way in.
---

import { Callout } from "nextra/components";

# Extending

You can add capabilities to Platypus without forking it. There are two routes
in, and picking the wrong one costs you a rebuild.

An **MCP server** adds tools at runtime, from outside Platypus, configured per
Workspace in the app. A **Plugin** is an npm package the Operator installs at
deploy time; it exports a manifest whose `contributes` block fills one or more
**Extension points** that core owns.

## Which one do you want

| You want to…                                                               | Reach for                                                             | Because                                                                       |
| -------------------------------------------------------------------------- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| Let an Agent call your internal service, or a third-party API              | **MCP server**                                                        | No code in Platypus, no restart, configured per Workspace by whoever needs it |
| Ship tools that are part of the product for everyone on the deployment     | **[Tool set](/extending/tool-sets)** contribution                     | One install, one switch on the Agent form, available to every Workspace       |
| Run an Agent's shell and filesystem tools somewhere new — Modal, E2B, a VM | **[Sandbox backend](/extending/sandbox-backends)** contribution       | Core owns the five tools; you supply the environment they run in              |
| Serve the Chat **Search** toggle from your own search service              | **[Web-search backend](/extending/web-search-backends)** contribution | Core owns the tools and the toggle; you supply the calls to your upstream     |

If you are integrating an external service for your own use, you almost always
want an MCP server. Reach for a Plugin when the capability should ship _with_
the deployment rather than be configured in it.

<Callout type="info">
  Both Sandbox backends and Tool sets already ship as core plugins —
  `@platypus/tools-basic`, `@platypus/tools-platform`, `@platypus/web-fetch`,
  `@platypus/docker` and `@platypus/ssh`. Yours loads through the same manifest
  path. The inventory an Operator controls is in [Configuration →
  Plugins](/self-hosting/configuration#plugins).
</Callout>

## Start here

**[Your first plugin](/extending/your-first-plugin)** is a runnable quickstart
off the example package in the repository. It ends with three checkpoints, so you
find out whether it worked rather than guessing.

Then the page for the Extension point you're filling —
**[Tool sets](/extending/tool-sets)**,
**[Sandbox backends](/extending/sandbox-backends)** or
**[Web-search backends](/extending/web-search-backends)** — and
**[Plugin API & config](/extending/plugin-api-and-config)** for the version
window, the append-only rule, and deploy-time config and credentials.

## Types and signatures

The compile-time contract lives in the published
[`@platypuschat/plugin-sdk`](https://www.npmjs.com/package/@platypuschat/plugin-sdk)
package: the manifest type, every Contribution type, and `PLUGIN_API_VERSION`.
These pages state the rules those types don't encode — what must be idempotent,
what the caps are, what happens at boot. For the exact shape of an interface,
read it from the package you have installed. A signature pasted onto a docs page
drifts silently; the one in your `node_modules` cannot.

Every Contribution is validated before it is registered. A Tool set with a
blank `id`, `name`, or `category`, or with `tools` that is neither a map nor a
factory, aborts startup with an error naming the Plugin and the entry that
failed. The package types catch these mistakes in TypeScript; the same checks
protect deployments that load JavaScript or a malformed package.

Strict at boot, forgiving at runtime: once loaded, a Contribution that fails
during a Chat turn — a Tool set factory that throws, a Web-search backend that
times out — costs that turn its own tools and is logged against your Plugin. It
never fails the turn, so one broken Plugin cannot take a deployment's chats down
with it.

Contribution ids are trimmed before they are namespaced, so surrounding
whitespace never reaches a stored id.

A third-party Plugin's tool **names** are namespaced too, under its manifest
`name`, so core's own turn tools can never overwrite one of yours. That puts a
length cap on both halves of the composed name, refused at boot — see [Tool
names](/extending/tool-sets#tool-names).

## Logging

A Plugin does not bring its own logger. Core puts one on the deploy-time block it
hands every contribution factory, already tagged with your Plugin's name, and the
Operator's `LOG_LEVEL` decides which of your lines are written — the same control
they have over core's. [Plugin API &
config](/extending/plugin-api-and-config#logging) has the shape and the levels.

The bundled Docker and SSH Sandbox backends write through this same logger, so
what you are given is what the two most involved Plugins in the box get. If it
turns out not to be enough for something you are building, that is a gap worth
reporting rather than a reason to reach for `console`.
