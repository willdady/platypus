---
title: Tool sets
description: Contribute a named group of tools that Agents can be granted, as a static map or a per-turn factory.
---

import { Callout } from "nextra/components";

# Tool sets

A **Tool set** is a named, categorised group of tools a Workspace Owner can
grant to an Agent with one switch. Contributing one puts it on the Agent form
for every Workspace on the deployment.

You add entries to `contributes.toolSets` in your manifest. Core reads the
manifest and drives registration itself — there is no `register*()` function for
you to call.

## The contribution

Each entry is a `ToolSetContribution` from
[`@platypuschat/plugin-sdk`](https://www.npmjs.com/package/@platypuschat/plugin-sdk).
It carries an `id`, a display `name`, a `category` that groups it on the Agent
form, an optional `description`, and `tools`. The exact signatures live in the
package; the rules below are what those types don't encode.

An individual tool is a Vercel AI SDK tool — `tool()` from the `ai` package,
with a Zod `inputSchema` and an `execute` function.

Pick `id`, `name` and `category` deliberately: the `id` is what gets persisted on
every Agent that is granted the set, and `name`, `category` and `description` are
what a Workspace Owner sees on the Agent form. Your tool names show too, in Chat.

<Callout type="warning">
  Changing an `id` after release orphans every Agent granted the old one. The
  grant is stored by id, and a turn that can't resolve the id degrades to no
  tools rather than erroring — so the symptom is an Agent that quietly stopped
  being able to do its job. Treat a shipped `id` as permanent. The same goes for
  your manifest `name`: the stored id is prefixed with it, so renaming the Plugin
  orphans every grant of every set it contributes.
</Callout>

## Static map or factory

`tools` is either a map of tools, or a function core calls at Chat-turn time.

A **static map** is right when the tools need nothing from the turn:

```ts
{
  id: "math-conversions",
  name: "Math Conversions",
  category: "Math",
  description: "Temperature and unit conversions",
  tools: { convertTemperature, convertDistance, convertWeight, convertVolume },
}
```

A **factory** is right when they need to know where they are running. It receives
a `ToolSetContext` carrying the Organization, Workspace, Agent and User ids for
this turn, plus the frontend URL if one is configured:

```ts
{
  id: "kanban",
  name: "Kanban",
  category: "Productivity",
  description: "Manage kanban boards in this workspace",
  tools: ({ workspaceId, agentId, orgId, frontendUrl }) =>
    createKanbanTools(workspaceId, agentId, orgId, frontendUrl),
}
```

The factory runs **once per turn**, not once at boot, so it may be `async` and
may read the database. Use `frontendUrl` to put a clickable link to a resource in
a tool's output — it is `undefined` when the deployment hasn't set one, so treat
the link as optional.

A factory also receives the Plugin's deploy-time config as an appended second
argument — see
[Plugin API & config](/extending/plugin-api-and-config#deploy-time-config-and-credentials).
That same argument carries the [logger](/extending/plugin-api-and-config#logging)
core binds to your Plugin's name, which is a reason to reach for a factory even
where a static map would otherwise do: a static map is handed nothing, so it has
nowhere to log from.

## Ids and namespacing

Write **bare** ids. Core prefixes a third-party Plugin's ids with its manifest
`name` at load, so `greeting` in a Plugin named `example` registers as
`example.greeting`. Core Plugins keep their ids flat (`kanban`, `time`,
`web-fetch`).

That split is what stops a third-party id colliding with a core one a later
release introduces. It also means the namespaced id is the real id: an Agent
granted the set above stores `example.greeting`, and that is what a turn
resolves.

Membership of the built-in map in the backend — not the package scope — decides
what counts as core, so publishing under an `@platypus/*` name you don't own
gains nothing. It is still prefixed as third-party.

## Degrading gracefully

A Tool set whose dependencies aren't there at turn time should resolve to **no
tools**, not throw. The core `sandbox` set is the model: in a Workspace with no
Sandbox configured it returns an empty map and the turn proceeds without shell
tools. Boot is strict; runtime is forgiving.

If a factory throws anyway, core catches it: the turn serves none of that set's
tools, logs a warning naming your Plugin and the set, and carries on with
everything else the Agent was granted. One broken Tool set costs its own tools,
never the chat. Returning an empty map is still the better habit — the outcome
is the same, and you get to say _why_ through the
[logger](/extending/plugin-api-and-config#logging).

## Releasing what a factory opened

A factory runs once per turn. If it opens something with a lifetime — a
connection, a pool, a watcher — register a way to close it:

```ts
{
  id: "kanban",
  name: "Kanban",
  category: "Productivity",
  tools: (ctx) => {
    const client = connect(ctx.workspaceId);
    ctx.registerCloser(() => client.close());
    return createKanbanTools(client);
  },
}
```

Core runs your closer once, when the turn ends — on a normal finish and on a
cancellation alike. A closer that throws is logged against your Plugin and the
rest still run; one that never settles is abandoned after **5 seconds**, because
teardown happens while the reader is still waiting on the reply. Registering the
same function twice within one turn registers it once — but registration does not
carry across turns, so register what this turn opened rather than something you
mean to keep between them.

There is no matching deadline on the factory itself: a Tool set declares no
per-call budget, and factories legitimately reach a database or a Sandbox on the
way to building their tools. Teardown gets a ceiling because core knows what it
is delaying; your factory's ceiling is a number only you know.

<Callout type="info">
  A static map is handed nothing, so it has nowhere to register from — the same
  reason it has nowhere to log from. Use a factory if the set owns anything that
  needs closing.
</Callout>

## Tool names

Write **bare** tool names, the same way you write bare ids. Core namespaces them
for you: every tool a third-party Tool set contributes reaches the model as
`<manifest-name>__<toolName>`.

```
you write:   createIssue
model calls: widgets__createIssue
a User sees: Widgets create issue
```

That happens on every turn, whether or not anything collides, so a tool's name
never depends on the order an Agent's Tool sets happen to sit in. It is also what
stops core taking one of your names: a turn assigns `web_search`, `read_url`,
`delegate` and `loadSkill` onto its tool map *after* your Tool set, so a set
contributing a bare `web_search` used to lose it on every turn where search was
on, with nothing anywhere saying so.

Do not hand-prefix. `acmeCreateIssue` in a Plugin named `acme` reaches the model
as `acme__acmeCreateIssue`, which reads worse and spends the budget below twice.
Core Plugins are the exception and keep bare tool names, the same way they keep
bare ids.

Keep to letters, digits, underscores and hyphens. That is the character set a
model provider accepts for a tool name, and a name carrying anything else fails
boot alongside the caps below.

### Two length caps

Because the two halves compose into one name, each half is capped:

- **The manifest `name`** — up to 24 characters.
- **A tool name** — up to 32 characters.

Both are checked at boot for a third-party Plugin. The tool-name cap holds
however short your manifest `name` is: a Plugin named `linear` does not get the
characters it left unused. Together they keep the composed
name inside what a model provider allows, so you never have to do that arithmetic
yourself.

<Callout type="warning">
  Either cap, exceeded, **aborts startup**. A statically-declared tool name over
  its cap is reported with your Plugin, the Tool set, the tool, the cap and the
  actual length; a manifest `name` over its cap with your Plugin, the field, the
  cap and the actual length. Nothing is truncated to fit — two long names from
  one set could truncate onto each other and quietly become one tool.
</Callout>

A **factory** resolves its names per turn, so boot cannot see them. A name over
the cap, or carrying a character the rule above forbids, is left out of the turn
with a warning, and the rest of the set is served. Keep a factory's names inside
both rules.

### What can still collide

Two Tool sets from the **same** Plugin that define one tool name still contest
it: they share a namespace, so the later one wins and core logs a warning naming
both sets. That is within your own control — give them distinct names.

An MCP can share your namespace too. Core prefixes each MCP tool with the MCP's
name — lowercased, spaces and punctuation turned to `_` — and the same `__`, so
an MCP a Workspace Owner names `Linear` enters a turn under the same prefix as a
Plugin named `linear`. When an Agent is granted both, a tool name they share goes
to whichever comes later in the Agent's list, and core logs a warning. Renaming
the MCP clears it.

## Adding one to a core Plugin

If you are working in the repository rather than shipping your own package, a
new core Tool set is a `ToolSetContribution` added to an existing core Plugin's
manifest:

- a Platypus-domain set belongs in `@platypus/tools-platform`;
- a pure utility belongs in `@platypus/tools-basic`;
- a capability an Operator would plausibly want to **deny on its own** — network
  egress, infrastructure reach — gets its own Plugin, so it can be left out of
  `PLATYPUS_PLUGINS`. `@platypus/web-fetch` is the pattern.

The first two are always-on and need nothing further. A new Plugin has to be
named in the deployment's `PLATYPUS_PLUGINS` before it loads at all.

<Callout type="info">
  Icons and custom in-chat rendering for a tool are a frontend concern and live
  outside the manifest. A tool with no custom UI falls back to a generic
  input/output renderer, which is usually fine.
</Callout>

## Where to next

- **[Your first Plugin](/extending/your-first-plugin)** — load one end to end.
- **[Plugin API & config](/extending/plugin-api-and-config)** — the deploy-time
  config a factory can read.
- **[Tool sets](/building-with-platypus/tool-sets)** — what the built-in sets do,
  from the Workspace Owner's side.
