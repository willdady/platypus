// Administering section order. Audience: Org Admin — the person who holds the
// credentials and decides what each Workspace can see. Ordered as the job comes
// up: get a model working, hand resources out, then tune each Workspace.
const meta = {
  index: "Overview",
  "shared-resources": "Shared resources",
  workspaces: "Workspace settings",
};

export default meta;
