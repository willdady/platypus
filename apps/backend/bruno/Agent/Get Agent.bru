meta {
  name: Get Agent
  type: http
  seq: 2
}

get {
  url: {{host}}/organisations/:orgId/workspaces/:workspaceId/agents/:agentId
  body: none
  auth: inherit
}

params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
  agentId:
}
