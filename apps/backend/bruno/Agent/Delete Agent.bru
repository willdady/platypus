meta {
  name: Delete Agent
  type: http
  seq: 5
}

delete {
  url: {{host}}/organisations/:orgId/workspaces/:workspaceId/agents/:agentId
  body: none
  auth: inherit
}

params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
  agentId:
}
