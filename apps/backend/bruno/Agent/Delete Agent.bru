meta {
  name: Delete Agent
  type: http
  seq: 5
}

delete {
  url: {{host}}/organizations/:orgId/workspaces/:workspaceId/agents/:agentId
  body: none
  auth: inherit
}

params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
  agentId:
}
