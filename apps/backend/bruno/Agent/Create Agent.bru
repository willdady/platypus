meta {
  name: Create Agent
  type: http
  seq: 3
}

post {
  url: {{host}}/organizations/:orgId/workspaces/:workspaceId/agents
  body: json
  auth: inherit
}

params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
}

body:json {
  {
    "workspaceId": "{{workspaceId}}",
    "providerId": "provider-id-here",
    "name": "My agent",
    "description": "A helpful assistant",
    "systemPrompt": "You are a helpful assistant.",
    "modelId": "google/gemini-2.5-flash",
    "toolSetIds": ["math-conversions"]
  }
}
