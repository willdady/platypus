meta {
  name: List Agents
  type: http
  seq: 1
}

get {
  url: {{host}}/organizations/:orgId/workspaces/:workspaceId/agents
  body: none
  auth: inherit
}

params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
}

