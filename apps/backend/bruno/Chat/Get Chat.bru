meta {
  name: Get Chat
  type: http
  seq: 2
}

get {
  url: {{host}}/organizations/:orgId/workspaces/:workspaceId/chat/:chatId
  body: none
  auth: inherit
}


params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
  chatId:
}
