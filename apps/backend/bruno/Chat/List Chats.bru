meta {
  name: List Chats
  type: http
  seq: 1
}

get {
  url: {{host}}/organizations/:orgId/workspaces/:workspaceId/chat
  body: none
  auth: inherit
}

params:query {
  ~search:
}

params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
}
