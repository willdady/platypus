meta {
  name: Update Chat
  type: http
  seq: 4
}

put {
  url: {{host}}/chat/:id?workspaceId=default
  body: json
  auth: inherit
}

params:query {
  workspaceId: default
}

params:path {
  id: 
}

body:json {
  {
    "title": "My Renamed Chat"
  }
}
