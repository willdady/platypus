meta {
  name: Generate Metadata
  type: http
  seq: 6
}

post {
  url: {{host}}/chat/:id/generate-metadata?workspaceId=default
  body: json
  auth: inherit
}

params:query {
  workspaceId: default
}

params:path {
  id: 
}

body:json {
  {
    "providerId": "provider-123"
  }
}
