meta {
  name: Generate Metadata
  type: http
  seq: 6
}

post {
  url: {{host}}/organizations/:orgId/workspaces/:workspaceId/chat/:chatId/generate-metadata
  body: json
  auth: inherit
}


params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
  chatId:
}

body:json {
  {
    "providerId": "provider-123"
  }
}
