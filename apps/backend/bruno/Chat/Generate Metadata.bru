meta {
  name: Generate Metadata
  type: http
  seq: 5
}

post {
  url: {{host}}/chat/:id/generate-metadata?workspaceId={{workspaceId}}
  body: json
  auth: none
}

body:json {
  {
    "providerId": "provider-123"
  }
}

query:json {
  {
    "workspaceId": "default"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}