meta {
  name: Delete Chat
  type: http
  seq: 5
}

delete {
  url: {{host}}/organisations/:orgId/workspaces/:workspaceId/chat/:chatId
  body: none
  auth: inherit
}


params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
  chatId:
}
