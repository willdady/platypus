meta {
  name: Delete Chat
  type: http
  seq: 5
}

delete {
  url: {{host}}/organizations/:orgId/workspaces/:workspaceId/chat/:chatId
  body: none
  auth: inherit
}


params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
  chatId:
}
