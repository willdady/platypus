meta {
  name: Delete Chat
  type: http
  seq: 5
}

delete {
  url: {{host}}/chat/:id?workspaceId=default
  body: none
  auth: inherit
}

params:query {
  workspaceId: default
}

params:path {
  id: 
}
