meta {
  name: List Providers
  type: http
  seq: 1
}

get {
  url: {{host}}/organizations/:orgId/workspaces/:workspaceId/providers
  body: none
  auth: inherit
}

params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
}

