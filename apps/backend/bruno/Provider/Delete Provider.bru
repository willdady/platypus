meta {
  name: Delete Provider
  type: http
  seq: 4
}

delete {
  url: {{host}}/organisations/:orgId/workspaces/:workspaceId/providers/:providerId
  body: none
  auth: inherit
}

params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
  providerId:
}
