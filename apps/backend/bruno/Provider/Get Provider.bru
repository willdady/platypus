meta {
  name: Get Provider
  type: http
  seq: 2
}

get {
  url: {{host}}/organisations/:orgId/workspaces/:workspaceId/providers/:providerId
  body: none
  auth: inherit
}

params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
  providerId:
}
