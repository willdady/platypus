meta {
  name: Get Provider
  type: http
  seq: 2
}

get {
  url: {{host}}/organizations/:orgId/workspaces/:workspaceId/providers/:providerId
  body: none
  auth: inherit
}

params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
  providerId:
}
