meta {
  name: Get MCP
  type: http
  seq: 2
}

get {
  url: {{host}}/organisations/:orgId/workspaces/:workspaceId/mcps/:mcpId
  body: none
  auth: inherit
}

params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
  mcpId:
}
