meta {
  name: Delete MCP
  type: http
  seq: 5
}

delete {
  url: {{host}}/organisations/:orgId/workspaces/:workspaceId/mcps/:mcpId
  body: none
  auth: inherit
}

params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
  mcpId:
}
