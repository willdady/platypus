meta {
  name: Update MCP
  type: http
  seq: 4
}

put {
  url: {{host}}/mcps/:id
  body: json
  auth: inherit
}

params:path {
  id: 
}

body:json {
  {
    "name": "My Updated MCP Server",
    "url": "https://example.com/mcp/v2",
    "authType": "Bearer",
    "bearerToken": "updated-token"
  }
}
