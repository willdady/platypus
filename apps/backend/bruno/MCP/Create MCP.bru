meta {
  name: Create MCP
  type: http
  seq: 3
}

post {
  url: {{host}}/organizations/:orgId/workspaces/:workspaceId/mcps
  body: json
  auth: inherit
}

params:path {
  orgId: {{orgId}}
  workspaceId: {{workspaceId}}
}

body:json {
  {
    "workspaceId": "{{workspaceId}}",
    "name": "My MCP Server",
    "url": "https://example.com/mcp",
    "authType": "Bearer",
    "bearerToken": "your-token-here"
  }
}
