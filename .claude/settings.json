{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "filepath=$(jq -r '.tool_input.file_path') && case \"$filepath\" in *.ts|*.tsx|*.md) npx prettier --write \"$filepath\" ;; esac"
          }
        ]
      }
    ]
  }
}
